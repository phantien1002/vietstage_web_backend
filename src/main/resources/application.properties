spring.application.name=vietstage_web_be
server.port=${PORT:9191}

spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USER:postgres.kzjdtnyxhnpqsfdprvrv}
spring.datasource.password=${DB_PASSWORD:1000Vietstage}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Redis Configuration
spring.data.redis.host=${REDIS_HOST:cobalt-wine-jar-78256.db.redis.io}
spring.data.redis.port=${REDIS_PORT:18232}
spring.data.redis.password=${REDIS_PASSWORD:NgCV6O4lWclpCBtcrMJvp9WO4U2Dpn2F}

# Auth/JWT Configuration
app.jwt.secret=${JWT_SECRET:vietstage_secret_key_fixed_length_for_hmac_sha256_must_be_long_enough}
app.jwt.access-token-expiration-ms=3600000
app.jwt.refresh-session-expiration-ms=604800000
springdoc.swagger-ui.path=/swagger-ui.html

springdoc.api-docs.path=/api-docs


spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=1
# Swagger UI Customization

# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=taintse183488@fpt.edu.vn
spring.mail.password=yusc ahys dwpa ecna
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

